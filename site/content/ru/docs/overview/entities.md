---
title: "Компоненты ROSS"
description: ""
lead: ""
draft: false
images: []
menu:
  docs:
    parent: "overview"
weight: 110
toc: true
---

## Сервис{#service}

Итак, помня про пластичность, наглядность и тестируемость, мы далее попытаемся построить воспроизвести процесс эволюции архитектуры, которая на практике доказала свою жизнеспособность. Во-всяком случае, как минимум один мой проект, где она применена, по-прежнему жив! Начнем с основы, ибо вначале была функция. Точнее, вначале были данные, т.к. без данных функция не имеет смысла. С другой стороны, если нет функции, откуда взялись данные? Чтобы не погружаться в проблему курицы и яйца, объединим функцию и данные и будем считать, что одно вытекает из другого, они неразрывно связаны, т.е.  вначале был объект. Физики тоже  так выкручиваются, когда их спрашивают «а из чего состоят кварки?», значит, и нам можно. Будем также считать, что объект — это состояние (кучка данных) и набор методов (функций), которые с этим состоянием умеют работать. Чтобы не навлечь на себя гнев адептов ООП за столь вольную трактовку Парадигмы,  сформулируем отдельный термин: «Сервис».

{{<alert context="info" icon="👉" text="Сервис — это объект, который выполняет задачи, определенные в рамках своей зоны ответственности, реагируя на сообщения." />}}
{{<alert context="info" icon="👉" text="Сообщение — это обращение к методу или свойству того или иного сервиса." />}}

Примерами сервисов являются локальное хранилище и сетевой слой. Отличительной особенностью сервиса является наличие императивного интерфейса и внутреннего состояния. Сервис может предоставлять доступ к своему состоянию синхронно, работать в режиме запрос-ответ, а также предоставлять подписки на обновления данных. В ходе выполнения задач сервис может обращаться к другим сервисам, которые доступны ему в виде зависимостей. Совокупное состояние всех сервисов формирует состояние приложения. Сервис — это первый элемент *базиса*.

Сервис бесполезен, если им никто не пользуется. Значит внешняя по отношению к сервису среда должна его инициализировать, а затем пинговать сообщениями. Сообщения также не берутся из ниоткуда — их порождают другие сервисы. Исключение — самый первый сервис-патриарх UIApplicationMain, который параметризируется  с помощью AppDelegate и запускается из функции main (видимо, все-таки вначале была функция): 
```
#import "AppDelegate.h"
int main(int argc, char *argv[])
{
    @autoreleasepool {
        return UIApplicationMain(argc, argv, nil, NSStringFromClass([AppDelegate class]));
    }
}
```
Однако обычно в этот код мы не заглядываем, поэтому сервисом-праотцом будем считать AppDelegate. У него даже нет конструктора (инициализатора) — идеальный кандидат на роль сущности, появившейся из небытия! Единственный его недостаток как сервиса в том, что все свои зависимости он инициализирует самостоятельно, что в общем случае приводит к невозможности «честно» покрыть его Unit-тестами. С другой стороны, этот класс настолько плотно увязан с окружением, т.е. операционной системой, что тестировать его нет смысла. Проще превратить его в тривиальный прокси-объект, вся ответственность которого будет заключаться в инициализации фабрик объектов (о них чуть позже) и передаче системных событий другим специализированным сервисам. Будем считать это допущение первым элементов в списке важных мелочей.

Итак, AppDelegate порождает другие сервисы и затем последовательно обращается к свойствам и методам этих сервисов, т.е. отправляет в сервисы сообщения. Логику этих обращений можно представить в терминах UML на диаграмме последовательности.  Мне больше нравится название «Диаграмма сообщений». На данном этапе мы обеспечили пластичность (легко добавлять новые сервисы) и сохранили наглядность (бизнес-логика представлена в диаграмме сообщений), а вот тестируемость обеспечена лишь частично. Unit-тесты на сервисы мы можем написать легко, а вот AppDelegate не тестируется, хотя и содержит бизнес-логику. Значит, нужно перенести эту бизнес-логику в тестируемую область! Встречаем новую составляющую базиса, она называется **операция**!

## Операция{#operation}

{{<alert context="info" icon="👉" text="Операция — это неделимая последовательность сообщений между сервисами." />}}

Как правило, операция представлена отдельным объектом, который получает в инициализаторе сервисы в качестве зависимостей. Основное отличие операции от сервиса в том, что она не имеет императивного интерфейса, т.е. не предоставляет доступа к своему состоянию и не имеет каких-либо методов в интерфейсе, кроме метода launch.  Метод  launch принимает на вход параметры, описывающие контекст запуска (переменные и/или дополнительные контекстные зависимости), а также замыкание onFinish, если операция асинхронная. Таким образом, операция похожа на обычную функцию: принимает параметры на вход и возвращает результат. Вместе с тем операция имеет свой жизненный цикл: ее нужно инициализировать, сохранить в памяти, запустить на выполнение, по завершению выполнения получить результат и затем удалить из памяти. Ссылку на асинхронную операцию нужно где-то хранить, чтобы после выхода из скоупа запущенная асинхронная операция не была удалена при подсчете ссылок. Я использую искусственный retain-cycle: при запуске операция создает сильную ссылку сама на себя, а при вызове onFinish освобождает ее. Добавим этот трюк в в список важных мелочей.

В документации операция представлена диаграммой сообщений, а в коде в виде цепочки обращений к сервисам. Диаграмма сообщений отвечает на вопрос «как делаем?», при этом ее объем ограничен рамками операции, что улучшает наглядность. Для того, чтобы обеспечить тестируемость, операции инициализируются в фабрике операций. Объект, который будет запускать операцию на выполнение, в данном случае AppDelegate, имеет доступ только к фабрике операций, но не к самой операции. Таким образом AppDelegate остается тривиальным, а сама операция легко покрывается Unit-тестами, поскольку все ее зависимости можно заменить mock-объектами, а через них отследить корректность обращений к сервисам. Добавим это решение в список важных мелочей. Для обеспечения пластичности в фабрику операций передается контейнер с ключевыми сервисами. Термин "ключевой" применительно к сервису означает, что время жизни сервиса сравнимо со временем жизни приложения. Доступ к контейнеру позволяет фабрике легко менять набор зависимостей у любой операции, следовательно, легко менять и логику операции. Добавим этот пункт в список важных мелочей. 

Итак, сервис использует фабрику операций, чтобы запускать операции на выполнение. При этом фабрика операций должна получить доступ к контейнеру с сервисами. Для того, чтобы обойти эту циклическую зависимость, предоставим AppDelegate возможность создать две особых операции без применения фабрики. Первая операция makeServicesOp будет отвечать за создание объектов сервисов — на выходе этой операции получим контейнер с ключевыми сервисами. Этот контейнер AppDelegate и передаст в фабрику операций. Вторая операция setupServicesOp отвечает за настройку контекстных зависимостей между сервисами. Еще один пункт в  список важных мелочей. Есть два вида зависимостей между сервисами: жесткие и контекстные. Жесткие передаются в конструктор и формируются в ходе makeServicesOp, а контекстные подключаются в ходе выполнения setupServicesOp. Потенциально любую жесткую зависимость можно превратить в контекстную, а вот наоборот — не всегда, т.к. есть вероятность получить циклическую зависимость. Контекстные зависимости нужно покрывать тестами. Еще один пункт в  список важных мелочей. 

Помимо ключевых сервисов в приложении также используются контекстные сервисы. Самый яркий представитель контекстных сервисов — это вью-модель. Как мы помним, отличительной характеристикой сервиса является императивный интерфейс. Интерфейс вью-модели обрабатывает команды, получаемых с UI-слоя, а также предоставляет доступ к данным, отображаемым на UI-слое. Вью-модель формируется непосредственно перед показом соответствующего экрана с учетом контекста, поэтому она называется контекстным сервисом. Помимо времени жизни контекстный сервис отличается от ключевого еще тем, что у первого нет доступа к фабрике операций. Операция непосредственно (без использования фабрик) инициализирует вью-модель, передает ей на вход все необходимые ключевые сервисы, а также подключает слушателей к событиям модели, т.е. формирует контекстные связи вью-модели с окружающим миром. Некоторые из этих событий приводят к вызову onFinish операции, что приводит к уничтожению и самой вью-модели. Это означает, что мы по-прежнему можем тестировать операцию, отслеживая ее обращения к ключевым сервисам. Протестировать вью-модель тоже легко, но как протестировать связку между вью-моделью и операцией? И мы вплотную подходим к следующем элементу базиса  — это **роутер**.

## Роутер{#router}

{{<alert context="info" icon="👉" text="Роутер — это сервис, который отвечает за весь UI-слой." />}}

Роутер выделен в отдельную сущность базиса, поскольку одна из важнейших мелочей звучит так: есть бизнес-логика и есть UI-логика, и они могут быть независимы. Обработка позиции скролла, анимации, навигационные переходы — это все UI-логика. В UI сосредоточена большая часть платформенных различий между клиентскими приложениями. Каждая платформа обладает собственным инструментарием для работы с UI, поэтому роутер — это самая платформозависимая сущность. Вместе с тем, у роутера есть императивный интерфейс, присущий сервису. Формально все сообщения для роутера выглядят так: «Покажи такой-то экран, вот тебе вью-модель с контекстом». Задача роутера — обработать предоставленный контекст и вывести результат на экран. Применительно к iOS-разработке это означает, что все UIViewController и UINavigationController живут исключительно в пределах роутера, т.е. все остальное приложение не знает о UI вообще ничего. Роутер — это просто сервис, который выводит изображение на экран и принимает через этот экран команды пользователя (касания). Роутер кадинально меняет подход к разработке клиентских приложений. Разработчик перестает мыслить в терминах экранов и переключает фокус на бизнес-логику. Может ли приложение вообще работать без UI? Конечно может! Если мы вместо честного роутера с вью-контроллерами под капотом подсунем в приложение через интерфейс роутера некий сетевой RPC-адаптер, то приложением можно будет управлять через удаленный терминал! После запуска на экране девайса будет пустой черный или белый экран, но при этом приложение будет работать как прежде! Это открывает невероятные возможности для высокоуровневых интеграционных тестов, не говоря уже о кроссплатформе. Да, кроссплатформа — это зло, но народная мудрость гласит «От тюрьмы, да от сумы... и от кроссплатформы не зарекайся!»


Итак, во главе угла теперь не экраны-модули, а бизнес-логика, для которой важно обеспечить пластичность, наглядность и тестируемость. Проверяем:
1. Тестируемость — обеспечена, т.к. закрыли проблему связки между вью-моделью и операцией. Операция обращается к роутеру как к еще одному сервису, передавая ему на вход вью-модель. RouterMock воздействует на вью-модель, и корректность настройки контекстных  связей между вью-моделью и ключевыми сервисами отслеживаем через обращения к соответствующим mock-объектам. Еще один пункт в копилку важных мелочей.
2. Наглядность — обеспечена, т.к. вью-модель изолирует бизнес-логику от UI-логики, а операция по-прежнему может быть описана диаграммой сообщений. В диаграмме появляется еще один сервис — роутер. Еще один пункт в копилку важных мелочей.
3. Пластичность — обеспечена, т.к. мы можем из любой операции запросить у роутера показ любого экрана. А вот это уже не мелочь — это роутер! Операция должна всего лишь предоставить контекст — вью-модель. Как обработать этот контекст — это уже забота роутера. Рассмотрим следующий пример.

У вас приложение-магазин. В приложении есть экран с каталогом товаров, экран с карточкой товара и экран настроек. Вам поступила задача от маркетинга: обрабатывать диплинк с акцией "Носки со скидкой 90%", т.е. уметь открывать карточку акционного товара в произвольный момент времени.  При этом требование UX-проектировщика — не прерывать пользовательский контекст при открытии акционной карточки. Это означает, что UI-логика открытия диплинка зависит от контекста. Если пользователь находится на вкладке с каталогом товаров, то акционную карточку следует открыть через push в NavigationController каталога, так же, как открываются все остальные карточки товаров. Если пользователь уже находится на карточке товара, то в случае совпадения текущей и акционной карточек ничего не делаем, а в случае различия снова выполняем push карточки в навигационный стек. Если пользователь находится на вкладке с настройками, где навигационного стека не предусмотрено, то карточку акционного товара показываем модально. За UI-логику такого рода отвечает роутер: у него есть свой внутренний контекст, который он самостоятельно сопоставляет с вызывающим контекстом во вью-модели, после чего принимает то или иное решение. Применительно к iOS инстанцирование вью-контроллеров можно перенести из роутера в фабрику вью-контроллеров. Таким образом в роутере останется "чистая" UI-логика, которую можно покрыть тестами точно так же, как это делается с любым другим сервисом.

Итак, мы обеспечили пластичность и тестируемость, наглядность на всех уровнях, включая UI-слой, а что насчет масштабирования? Действительно, приложение не может состоять из одной операции и набора сервисов. В примере с сервисом доступа к электронным книгам у нас изначально было 5 модулей: онбординг, авторизация, поиск книг в каталоге, просмотр выбранной книги и встроенные покупки. Естественным образом появляются 5 соответствующих операций: OnboardingOp, AuthOp, BookSearchOp, BookViewOp и BuyOp. AppDelegate создает и запускает операции, причем порядок их запуска определяется состоянием приложения и «межоперационной» бизнес-логикой. Например, мы запускаем OnboardingOp только при первом запуске приложения, а BookSearchOp запускается только в том случае, если пользователь авторизован. Если не авторизован, то следует запустить AuthOp. Логику запуска операций в терминах UML можно описать диаграммой деятельности. С учетом контекста статьи мне больше нравится название «Диаграмма операций». Диаграмма операций отвечает на вопрос «что делаем?», и ее вполне может сформировать не только системный аналитик, но и продакт на основе User Stories. Так мы обеспечиваем наглядность. Перед нами снова встает задача тестирования, на этот раз нужно протестировать «межоперационную» логику. Как и в случае с операцией, мы перенесем эту логику во внешнюю сущность и в результате получим последний, четвертый элемент базиса — **сценарий**.

## Сценарий{#scenario}

{{<alert context="info" icon="👉" text="Сценарий — это последовательность операций." />}}

Сценарии формируют каркас приложения. Можно изучить только эти классы и в общих чертах понять, как приложение устроено и какую функциональность оно предоставляет. Технически сценарий представляет собой объект, который создается и запускает на выполнение точно так же, как и операция. Отличие от операции в том, что у сценария есть доступ к фабрике операций, а у операции нет. Важная мелочь: как только у операции возникает потребность обратиться к другой операции, она превращается в сценарий. Операция неделима!

Сценарий отвечает за запуск операций, обработку результата их выполнения и передачу данных от одной операции к другой. Важная мелочь: сценарий формируется с помощью фабрики сценариев, а запустить сценарий на выполнение может только сервис, имеющий доступ к этой фабрике. Так мы обеспечиваем тестируемость и сценария, и запускающего его сервиса.Важная мелочь-следствие: если сценарию нужно запустить другой (параллельный) сценарий, то он обращается к сервису, который отвечает за запуск этого параллельного сценария. Соответственно, инициализатор такого сценария получает на вход не только фабрику операций, но также «сервис-запускатор». Назовем его AppService. Доступ к AppService позволяет сценарию оставаться пластичным.  AppDelegate может делегировать в AppService работу со сценариями, по-прежнему оставаясь тривиальным и нетестируемым. В терминах UML AppService легко описать диаграммой прецедентов. И это название хорошо отражает происходящее, поскольку AppService может запускать не только сценарии, но и одиночные операции, для которых создавать целый сценарий избыточно

Вернемся к истории про платежку на онбординге. Очевидно, что нам придется серьезно перестроить наше модульное приложение, но начнем мы с проектирования. Диаграмма прецедентов теперь включает 4 прецедента: вход, поиск в каталоге, просмотр книги, оплата. Вход представлен сценарием, включает в себя 3 операции: онбординг, авторизация и оплата. Поиск в каталоге — это единичная операция. Оплата — это тоже единичная операция, но лишь на первый взгляд. Во-первых, она теперь может запускаться как в авторизованном, так и неавторизованном состояниях. Во-вторых, она должна учитывать контекст — платил ли пользователь уже? Таким образом, оплата — это сценарий, который включает 3 отдельных операции: проверка статуса оплаты, оплата в авторизованном состоянии и оплата в неавторизованном состоянии. Следовательно, из сценария входа оплату убираем, теперь это параллельный сценарий. Просмотр книги — это сценарий, который включает 2 операции: проверка статуса оплаты (если не оплачено — запускает сценарий оплаты) и непосредственно просмотр книги.

Наш маркетолог продолжает генерировать идеи. На этот раз продуктовая гипотеза заключается в том, что если дать пользователю посмотреть каталог книг без регистрации, то это повысит конверсию в оплату. Регистрацию запрашивать только перед просмотром книги, при этом оплатить без регистрации по-прежнему можно! Снова начнем с проектирования. Диаграмма прецедентов теперь включает только 3 прецедента: вход и просмотр и оплата. Прецедент «вход»: теперь он включает 2 операции: онбординг и поиск в каталоге. Прецедент «просмотр» представлен сценарием из двух операций: авторизация и просмотр. Также в «просмотре» активируется сценарий оплаты. Как мы видим, правки свелись исключительно к манипуляциям внутри сценариев. Мы больше не привязаны к экранам, и свободно можем перекраивать приложение в практически неограниченных масштабах.

Итак, в ходе масштабирования нам удалось удовлетворить всем трем ключевым требованиям к архитектуре приложения. Тестируемость обеспечена: мы можем покрыть unit-тестами все составляющие приложения, включая UI-логику. Пластичность обеспечена: мы легко можем перестроить отдельные операции и сценарии, не прибегая к масшабному рефакторингу — зависимости всегда под рукой. Наконец, наглядность обеспечена диаграммами сообщений, операций, и прецедентов, которые однозначно отображаются в код. Диаграммы являются основной частью Технического задания, формируемого аналитиками и проектировщиками для программистов, а также являются "источником правды" для тестировщиков.

